# Environment Configuration

This file documents expected environment variables.
Copy this file to `.env` and fill in values as needed.

```bash
# Currently no required env vars for MVP
# Backend/API URLs will be added in future phases
```

For now, all data is stored locally using AsyncStorage.
